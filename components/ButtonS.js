const ButtonS = ({ title }) => {
  return (
    <button className="border-2 border-solid text-lg font-bold font-pop rounded-lg border-primary text-primary px-6 py-2 hover:font-bold hover:bg-primary hover:text-white">
      {title}
    </button>
  );
};

export default ButtonS;
